**Mã Sinh Viên:** 2A202600689
**Tên sinh viên:** Nguyễn Bình Huy

## Tổng Quan Kết Quả
| Tiêu chí | Trước khi tối ưu (`results_before.json`) | Sau khi tối ưu (`results_after.json`) |
| :--- | :--- | :--- |
| **Tổng điểm (Overall Score)** | **34.00 / 100** | **90.69 / 100** |
| **Tổng điểm thành phần** | 442.0 / 1300.0 | 1179.0 / 1300.0 |

## Nhận Xét & Phân Tích

### 1. Tình trạng ban đầu (Baseline)
- Agent liên tục bị lỗi luồng (Tool trace mismatch), thường xuyên bỏ qua các bước `get_discount` hoặc `calculate_order_totals`.
- Không tạo được file JSON lưu đơn hàng do format dữ liệu đầu vào sai lệch.
- Xử lý thiếu sót các trường hợp guardrails (cho phép fake invoice, bỏ qua lỗi stock).

### 2. Biện pháp cải thiện (Prompt Engineering & Schema)
- **Cải thiện System Prompt**: Viết lại prompt cực kỳ chặt chẽ, ép buộc Qwen phải kiểm tra thông tin khách hàng, đánh giá các rủi ro (bỏ qua stock, fake invoice) trước khi gọi bất kỳ tool nào. Quy định thứ tự gọi tool bắt buộc (list -> details -> discount -> totals -> save).
- **Thiết kế Permissive Tool Schema**: Áp dụng các hàm "ép kiểu" (`_coerce_object`, `_coerce_items`) giúp cứu những tham số đầu vào bị lỗi cú pháp của Qwen, giúp pipeline chạy xuyên suốt mà không bị crash.

### 3. Kết quả đạt được (After)
- **Valid Orders (Đơn hợp lệ)**: Các case đặt hàng bundle (gaming, workstation, v.v.) đạt 94-97 điểm. Tool gọi đúng thứ tự, JSON output lưu đúng định dạng.
- **Guardrails (Từ chối an toàn)**: Nhận diện và chặn đứng hoàn toàn các ca "fake invoice" hoặc "bỏ qua kiểm tra tồn kho", đạt trung bình 85-88 điểm.
- **Clarification (Hỏi thêm thông tin)**: Hỏi chính xác các trường hợp thiếu Email hoặc thiếu Địa chỉ giao hàng mà không bị kích hoạt tool sớm.

👉 **Kết luận**: Nhờ áp dụng các kỹ thuật Prompt Engineering chặt chẽ kết hợp với cơ chế nới lỏng input schema, Agent đã trở nên cực kỳ ổn định, đạt đúng tiêu chuẩn của Lab với số điểm ấn tượng.
